# Intended usage on local machine.

echo 'Pulling origin master' && git pull origin master

echo 'Copying local .bash_aliases' && cp /Users/zh/.bash_aliases .
echo 'Copying local .vimrc' && cp /Users/zh/.vimrc .
echo 'Copying local .gitconfig' && cp /Users/zh/.gitconfig .
